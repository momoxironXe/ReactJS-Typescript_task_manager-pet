export interface LoginFormType {
    email: string,
    password: string
}

export interface Task {
    title: string,
    description: string,
    data: number,
    from: string,
    status: boolean,
}